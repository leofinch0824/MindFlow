# 交互缺口与矛盾日志

| ID | 类型（缺失/冲突/体验断裂/契约不一致） | 证据 | 影响 | 建议 | 优先级 | 状态 |
|---|---|---|---|---|---|---|
| IA-BE-001 | 契约不一致 | `frontend/src/api/newsletter.ts:86-90,198-203` 将 `digestsApi.list` 声明为 `DigestListResponse`，`frontend/src/pages/Newsletter.tsx:84-87` 直接读取 `res.items.map(...)`；但 `backend/routers/digests.py:26-30` 的 `GET /api/digests` 声明为 `response_model=list[DailyDigestResponse]` 并直接返回数组。 | 日期下拉历史列表在拿到裸数组时会访问不存在的 `items`，异常被 `catch(() => {})` 吞掉后，日期候选可能一直为空。 | 前后端统一为同一列表契约：要么后端包装成 `{ items, total, limit, offset }`，要么前端改为按数组读取。 | 待 Task 5 定级 | Open |
| IA-BE-002 | 契约不一致 | `frontend/src/api/newsletter.ts:3-23` 的 `fetchApi` 在所有 2xx 响应上都执行 `res.json()`；`frontend/src/api/newsletter.ts:235-236` 的 `deleteTag` 复用该实现；但 `backend/routers/interests.py:114-122` 的 `DELETE /api/interests/tags/{tag_id}` 返回 `204 No Content`。`frontend/src/pages/InterestSettings.tsx:59-64` 只有在 `await interestsApi.deleteTag(id)` 成功返回后才会刷新数据。 | 后端删除成功时前端仍会因解析空响应体而抛错，导致删除后的刷新链路中断、界面可能停留在旧数据，且错误仅写入控制台。 | 统一删除契约：后端返回 JSON 成功体，或前端在 `fetchApi` 中对 204/空响应跳过 `res.json()`。 | 待 Task 5 定级 | Open |
| IA-BE-003 | 契约不一致 | `frontend/src/api/newsletter.ts:115-119,243-244` 将候选标签声明为 `TagCandidate { tag, avg_significance, count }`；`frontend/src/pages/InterestSettings.tsx:271-279` 至少依赖 `tag` 与 `count` 渲染；但 `backend/routers/interests.py:136-139` 仅声明 `GET /api/interests/candidates` 的 `response_model=list[dict]`，`backend/schemas.py` 中也没有候选标签响应模型。 | 候选标签接口缺少后端显式 schema 约束，字段改名/缺失不会被 FastAPI 校验，前端可能出现空标签、`undefined` 计数或静默渲染异常。 | 在 `backend/schemas.py` 中补充候选标签响应模型并绑定到 `response_model`，与前端 `TagCandidate` 字段保持一致。 | 待 Task 5 定级 | Open |
| IA-BE-004 | 契约不一致 | `frontend/src/api/newsletter.ts:75-84,210-214` 将 `digestsApi.generate` 声明为返回 `DailyDigest`；但 `backend/routers/digests.py:51-98` 的 `POST /api/digests/generate` 实际返回 `{ success, message, digest_id, overview, sections_count }` 状态对象，而非简报详情。 | 该接口一旦被前端页面接线，调用方若按 `DailyDigest.sections/title/date` 读取会直接读到缺失字段。 | 在接线前先统一返回契约：前端改类型为生成结果 DTO，或后端改为返回完整 `DailyDigestResponse`。 | 待 Task 5 定级 | Open |
| IA-BE-005 | 缺失 | `frontend/src/pages/Newsletter.tsx:194-199` 的点赞/点踩按钮没有 `onClick`；仓库已有 `behaviorApi.recordFeedback`（`frontend/src/api/newsletter.ts:261-264`）和后端 `POST /api/behavior/feedback`（`backend/routers/behavior.py:97-111`）。 | 用户对 Main Channel 的显式喜欢/不喜欢反馈不会落库，兴趣学习链路失去最直接的纠偏信号。 | 将按钮接入 `behaviorApi.recordFeedback`，并补齐 digest/anchor/action 的映射。 | 待 Task 5 定级 | Open |
| IA-BE-006 | 缺失 | `frontend/src/pages/Newsletter.tsx:201-203` 的 `Read Source` 仅执行外链跳转；行为埋点 hook `useBehaviorCollector` 暴露 `recordClick`（`frontend/src/hooks/useBehaviorCollector.ts:147-163`），其内部调用 `POST /api/behavior/logs`（`frontend/src/hooks/useBehaviorCollector.ts:41-59`；`backend/routers/behavior.py:30-77`）。 | 文章来源点击不会进入行为日志，后端无法把 click 信号计入标签权重，兴趣画像学习结果偏弱。 | 在来源链接点击前补发 click 行为日志，或把 `useBehaviorCollector` 接到可识别的 insight 组件上。 | 待 Task 5 定级 | Open |
| IA-BE-007 | 契约不一致 | `frontend/src/api/newsletter.ts:50-55` 的 `AIConfig` 类型不包含 `api_key`，`frontend/src/pages/Settings.tsx:22-32` 在 `GET /api/config/ai` 后把 `formData.api_key` 重置为 `''`，而 `frontend/src/pages/Settings.tsx:40-45` / `frontend/src/api/newsletter.ts:179-183` 会将整个 `formData` 直接提交到 `PUT /api/config/ai`；后端 `backend/routers/config.py:10-26` 的 `GET /api/config/ai` 使用 `AIConfigResponse`（`backend/schemas.py:80-84`）返回时省略 `api_key`，但 `backend/routers/config.py:29-37` 的 `PUT /api/config/ai` 依据 `AIConfigUpdate`（`backend/schemas.py:73-77`）要求并持久化 `api_key`。另见 `frontend/src/pages/Settings.tsx:54-59` 与 `backend/routers/config.py:41-45`：测试连接只验证后端当前已保存配置。 | 用户若未重新输入密钥就直接保存，已存储的 `api_key` 可能被空字符串覆盖；同时“Test Connection”验证的是后端现存配置而非未保存表单，可能掩盖待保存配置与实际测试对象不一致的问题。 | 将密钥更新改为显式可选语义：后端允许省略/保留已有 `api_key`，或前端在空值时不提交该字段，并在测试连接时明确区分“已保存配置”与“当前表单”。 | 待 Task 5 定级 | Open |
| IA-SPEC-001 | 冲突 | `docs/superpowers/specs/2026-04-10-product-requirements.md:390-397` 要求兴趣标签页展示系统推荐的新标签候选并支持一键采纳；但 `docs/superpowers/specs/2026-04-09-ai-news-digest-v2-technical-spec.md:181-194,352-375` 只定义了兴趣标签 CRUD / stats / feedback 的 API、Hook 与组件清单，没有候选标签接口或采纳交互。关联矩阵行: `InterestSettings / Add Interest 输入框 / Add Interest 按钮`、`InterestSettings / Emergent Patterns / Adopt`。 | 如果研发只按 TECH 落地，候选标签与一键采纳链路会被遗漏，无法满足 PRD 对“推荐逻辑解释页”的核心要求。 | `以 PRD 为准`，在 TECH 中补齐候选标签的 API、组件与一键采纳交互定义。 | 待 Task 5 定级 | Open |
| IA-SPEC-002 | 冲突 | `docs/superpowers/specs/2026-04-10-product-requirements.md:414-422` 要求信息源页支持“从链接尝试添加信息源”、手动触发抓取并查看来源状态；但 `docs/superpowers/specs/2026-04-09-ai-news-digest-v2-technical-spec.md:189-194,352-375` 仅列出 sources CRUD/types、`SourceItem` 与 `useSources`，未覆盖 parse-url、manual fetch 或状态字段契约。关联矩阵行: `Sources / 页脚 Quick Add 输入框 / 添加按钮`、`Sources / Source 行内 Fetch Now / Manual Crawl All`。 | 如果以 TECH 为唯一实现依据，Quick Add 解析、手动抓取与来源状态展示会缺少正式规格支撑，核心“来源可控”闭环会断裂。 | `以 PRD 为准`，在 TECH 中补齐链接解析、手动抓取、来源状态字段与对应交互说明。 | 待 Task 5 定级 | Open |
| IA-SPEC-003 | 冲突 | `docs/superpowers/specs/2026-04-10-product-requirements.md:441-445` 要求 AI 配置页支持查看/修改提供商、API 地址、模型、API Key、测试连接与保存；但 `docs/superpowers/specs/2026-04-09-ai-news-digest-v2-technical-spec.md:342-350,352-375` 仅给出 `/settings/ai` 页面路由，没有配置接口、字段语义或测试连接契约。关联矩阵行: `Settings / Settings 页面加载`、`Settings / Test Connection 按钮`、`Settings / Save Architecture 按钮`。 | 研发若只遵循 TECH，将无法从技术规格中得出 AI 配置页必须具备的字段和交互闭环，易导致实现收缩到“只有页面入口”。 | `以 PRD 为准`，补充 AI 配置读取/保存/测试接口与字段级行为定义。 | 待 Task 5 定级 | Open |
| IA-SPEC-004 | 冲突 | `docs/superpowers/specs/2026-04-10-product-requirements.md:462-472` 将无简报、无兴趣标签、无信息源、AI 未配置、抓取失败、测试连接失败、加载中列为必备状态，并要求空状态提示下一步；但 `docs/superpowers/specs/2026-04-09-ai-news-digest-v2-technical-spec.md:340-375` 的页面/组件/Hook 规格没有任何状态组件或状态处理约束。关联矩阵页: `Newsletter`、`InterestSettings`、`Sources`、`Settings`。 | 如果团队仅依赖 TECH，PRD 明确要求的空态/错态/加载态很容易在实现时被忽略，产品可理解性会直接下降。 | `以 PRD 为准`，在 TECH 中补充各页面必备状态、状态文案目标与触发条件。 | 待 Task 5 定级 | Open |
| IA-SPEC-005 | 冲突 | `docs/superpowers/specs/2026-04-10-product-requirements.md:363,508` 将“主航道 / 探索区 / 惊喜箱”定义为需要被强化的内容分层；但 `docs/superpowers/specs/2026-04-09-ai-news-digest-v2-technical-spec.md:309-322,356-363` 使用 `main / explore / discover` 区域枚举，组件清单也只显式列出 `ExploreSection`，没有 `惊喜箱` 对应的命名或展示约束。关联矩阵行: `Newsletter / Main Channel 来源链接`、`Newsletter / Main Channel 点赞 / 点踩按钮`。 | 规格命名不一致会让实现层与产品语言脱节，用户侧可能看不到 PRD 要求的“惊喜箱”分层，且前后端枚举/文案容易继续分叉。 | `以 PRD 为准`，统一 TECH 中的分层命名、枚举映射与对应展示组件说明。 | 待 Task 5 定级 | Open |
