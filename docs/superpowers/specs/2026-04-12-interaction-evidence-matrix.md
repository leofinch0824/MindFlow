# 交互证据矩阵

| 页面 | 控件/入口 | 用户动作 | 前端触发逻辑（文件:行） | 后端接口（Method Path） | 需求依据（文档:行） | 当前行为 | 判定 | 优先级 | 备注 |
|---|---|---|---|---|---|---|---|---|---|
| Newsletter | 日期选择器 / 今天 | 选择历史日期或今天 | `frontend/src/pages/Newsletter.tsx:84-88,109-123,275-285` | `GET /api/digests?limit=30`（`backend/routers/digests.py:26-30`，`response_model=list[DailyDigestResponse]`）；`GET /api/digests/latest`（`backend/routers/digests.py:33-39`）；`GET /api/digests/{date_str}`（`backend/routers/digests.py:42-48`） | PRD:346-347,374-376; TECH:175-178,343-345,357 | 日期下拉组件挂载时先拉取最近 30 天简报日期；选择某个日期/今天后设置 `selectedDate`，再请求对应日期简报；未选择日期时首屏请求 latest。 | Red | P1 | 历史日期链路受 `IA-BE-001` 影响；`GET /api/digests` 返回裸数组导致日期列表可能为空，头部左右箭头也未接线。 |
| Newsletter | 兴趣标签侧栏 | 点击标签筛选简报 | `frontend/src/pages/Newsletter.tsx:24-31,266-273,287-293` | 首屏 `GET /api/interests/tags`（`backend/routers/interests.py:54-58`，`response_model=list[UserInterestTagResponse]`）；点击标签后无新增后端请求 | PRD:349-351,497-503 | 页面首屏加载标签列表；点击标签只会更新 `selectedTag` 并在前端内存中过滤 `digest.sections[].insights`。 | Green | — | 前端内存过滤即可完成当前简报内筛选，选中态与结果列表同步反馈。 |
| Newsletter | Main Channel 来源链接 | 点击 `Read Source` | `frontend/src/pages/Newsletter.tsx:201-203` | 当前无后端调用；仓库内可用行为上报入口为 `POST /api/behavior/logs`（`backend/routers/behavior.py:30-77`） | PRD:350,352-353; TECH:179-180,187-188,360-375 | 直接打开外部文章来源（新标签页），不会记录前端状态或回传后端点击事件。 | Yellow | P1 | 原文跳转主路径可用，但点击信号未回流行为日志，学习链路少一类隐式反馈；不阻断当次阅读，见 `IA-BE-006` / `IA-SPEC-005`。 |
| Newsletter | Main Channel 点赞 / 点踩按钮 | 点击反馈按钮 | `frontend/src/pages/Newsletter.tsx:194-199` | 前端未接线；可用后端接口为 `POST /api/behavior/feedback`（`backend/routers/behavior.py:97-111`） | PRD:353,497-503; TECH:185-188,362,374-375 | 按钮仅渲染 UI，当前没有 `onClick` 处理函数，也不会调用任何行为/反馈接口。 | Red | P0 | 反馈按钮会误导用户为“可反馈”，但当前没有任何提交动作；命名分层也仍沿用 `Main Channel`，见 `IA-BE-005` / `IA-SPEC-005`。 |
| InterestSettings | Add Interest 输入框 / Add Interest 按钮 | 输入标签后回车或点击添加 | `frontend/src/pages/InterestSettings.tsx:32-44,157-168` | `POST /api/interests/tags`（`backend/routers/interests.py:82-91`，`response_model=UserInterestTagResponse`）；随后 `GET /api/interests/tags`（`backend/routers/interests.py:54-58`）、`GET /api/interests/tags/stats`（`backend/routers/interests.py:61-70`）、`GET /api/interests/candidates?top_n=5`（`backend/routers/interests.py:136-139`，`response_model=list[dict]`） | PRD:390-392,394-396,403-406; TECH:181-186,364-365,373 | 空字符串直接拦截；成功后清空输入框并重新加载标签 / 统计 / 候选数据。 | Green | — | 新增标签主路径闭合：空值拦截、创建成功后清空输入并刷新列表。 |
| InterestSettings | 兴趣卡片 Freeze 操作 | 点击 Freeze | `frontend/src/pages/InterestSettings.tsx:50-57,220-227` | `PATCH /api/interests/tags/{tag_id}`（`backend/routers/interests.py:94-111`，`response_model=UserInterestTagResponse`）；随后 `GET /api/interests/tags`（`backend/routers/interests.py:54-58`）、`GET /api/interests/tags/stats`（`backend/routers/interests.py:61-70`）、`GET /api/interests/candidates?top_n=5`（`backend/routers/interests.py:136-139`） | PRD:393-396,403-406; TECH:183,186,364-365,373 | 将标签状态更新为 `frozen`，成功后整页数据刷新。 | Green | — | 冻结请求与刷新链路闭合，列表状态会更新并反映到权重展示。 |
| InterestSettings | 冻结兴趣 Thaw / Activate 操作 | 点击 Thaw | `frontend/src/pages/InterestSettings.tsx:50-57,211-218` | `PATCH /api/interests/tags/{tag_id}`（`backend/routers/interests.py:94-111`，`response_model=UserInterestTagResponse`）；随后 `GET /api/interests/tags`（`backend/routers/interests.py:54-58`）、`GET /api/interests/tags/stats`（`backend/routers/interests.py:61-70`）、`GET /api/interests/candidates?top_n=5`（`backend/routers/interests.py:136-139`） | PRD:393-396,403-406; TECH:183,186,364-365,373 | 将标签状态更新为 `active`，成功后整页数据刷新。 | Green | — | 恢复激活链路可用；UI 文案为 `Thaw`，但实际状态更新为 `active`。 |
| InterestSettings | 兴趣卡片 Delete 操作 | 点击 Delete 并确认 | `frontend/src/pages/InterestSettings.tsx:59-67,228-233` | `DELETE /api/interests/tags/{tag_id}`（`backend/routers/interests.py:114-122`，`status_code=204`）；随后前端尝试 `GET /api/interests/tags` / `GET /api/interests/tags/stats` / `GET /api/interests/candidates?top_n=5` 刷新 | PRD:392; TECH:184,364-365,373 | 先弹浏览器 `confirm`；确认后删除标签并刷新数据。 | Red | P1 | 删除请求命中 `204 No Content` 契约缺口，后端成功后前端仍可能报错并停留旧数据；见 `IA-BE-002`。 |
| InterestSettings | Emergent Patterns / Adopt | 点击候选标签的 Adopt | `frontend/src/pages/InterestSettings.tsx:46-48,271-287` | `POST /api/interests/tags`（`backend/routers/interests.py:82-91`）；随后 `GET /api/interests/tags`（`backend/routers/interests.py:54-58`）、`GET /api/interests/tags/stats`（`backend/routers/interests.py:61-70`）、`GET /api/interests/candidates?top_n=5`（`backend/routers/interests.py:136-139`，`response_model=list[dict]`） | PRD:396-397,403-406; TECH:182,364-365,373 | `handleAddCandidate` 直接复用 `handleAddTag`；点击后把候选标签转成正式兴趣标签。 | Yellow | P2 | 采纳入口可复用新增标签主路径，但候选接口字段未被后端 schema 锁定；见 `IA-BE-003` / `IA-SPEC-001`。 |
| Sources | 页脚 Quick Add 输入框 / 添加按钮 | 粘贴公众号文章链接后回车或点击添加 | `frontend/src/pages/Sources.tsx:33-57,279-297` | `POST /api/sources/parse-url`（`backend/routers/sources.py:53-91`，`response_model=ParseUrlResponse`） | PRD:418,429-431; TECH:189-194,366-367,375 | 先做非空和 `mp.weixin.qq.com` 校验；解析成功后打开确认弹窗并注入 `parsedAccountForModal`，此步骤本身不创建 source。 | Green | — | 页脚入口具备非空/域名校验与解析错误反馈，成功后进入确认弹窗。 |
| Sources | Quick Add 弹窗 / 解析 | 在弹窗内点击解析 | `frontend/src/pages/Sources.tsx:483-506,541-562` | `POST /api/sources/parse-url`（`backend/routers/sources.py:53-91`，`response_model=ParseUrlResponse`） | PRD:418,429-431; TECH:189-194,366-367,375 | 当弹窗没有 `initialAccount` 时，用户可再次输入 URL 并重新解析公众号信息。 | Green | — | 弹窗内可重试解析并显示公众号资料，解析链路与结果反馈闭合。 |
| Sources | Quick Add 弹窗 / 确认添加 | 点击确认添加公众号 | `frontend/src/pages/Sources.tsx:508-526,627-633` | `POST /api/sources`（`backend/routers/sources.py:94-105`，`response_model=NewsSourceResponse`） | PRD:417-418,422,429-431; TECH:189-190,366-367,375 | 将解析出的公众号信息映射成 `mptext` source 并创建；成功后关闭弹窗并重新加载 source 列表。 | Green | — | 解析结果可直接落库为 source 并刷新列表，完成“从链接尝试添加”主路径。 |
| Sources | SourceModal / 新增来源保存 | 点击 Add New Source 或空状态 Start Connection 后提交保存 | `frontend/src/pages/Sources.tsx:116-119,177-180,355-370,457-461` | `POST /api/sources`（`backend/routers/sources.py:94-105`，`response_model=NewsSourceResponse`） | PRD:417,429-431; TECH:189-190,366-367,375 | 点击页头或空状态入口打开 `SourceModal`；提交时若 `source` 为空则创建 source，成功后关闭弹窗并重新加载列表。 | Green | — | 手动新增来源可提交并刷新列表，空状态入口与页头入口都能闭环完成创建。 |
| Sources | Source 行内 Edit Source | 点击编辑并保存 | `frontend/src/pages/Sources.tsx:82-85,244-250,355-368,457-461` | `PUT /api/sources/{source_id}`（`backend/routers/sources.py:117-128`，`response_model=NewsSourceResponse`） | PRD:419,422,428-430; TECH:191,366-367,375 | 行内编辑按钮打开 `SourceModal`；提交时若存在 `source` 则走更新接口。 | Green | — | 编辑保存路径可更新并回流列表，满足来源维护主任务。 |
| Sources | Source 行内 Delete Source | 点击删除并确认 | `frontend/src/pages/Sources.tsx:59-67,251-257` | `DELETE /api/sources/{source_id}`（`backend/routers/sources.py:131-138`） | PRD:420; TECH:192,366-367,375 | 先弹浏览器 `confirm`；确认后删除并直接用本地 `filter` 更新列表。 | Green | — | 确认后可删除并立即从列表移除，当前主路径闭合。 |
| Sources | Source 行内 Fetch Now | 点击单条抓取 | `frontend/src/pages/Sources.tsx:69-80,236-243` | `POST /api/sources/{source_id}/fetch`（`backend/routers/sources.py:141-153`，`response_model=FetchResponse`） | PRD:421-422,428-430; TECH:189-194,366-367,375 | 设置 `fetchingId` 后调用抓取接口；成功时弹出返回消息并重新拉取来源列表。 | Green | — | 单条抓取链路闭合，按钮 loading 态与成功后的列表刷新行为一致。 |
| Sources | 页头 Manual Crawl All | 点击批量抓取全部来源 | `frontend/src/pages/Sources.tsx:108-114` | `POST /api/sources/{source_id}/fetch`（`backend/routers/sources.py:141-153`，`response_model=FetchResponse`） | PRD:421-422,428-430; TECH:189-194,366-367,375 | 通过 `sources.forEach` 逐条调用单条抓取接口；按钮本身没有批量任务 ID、总进度或汇总结果反馈。 | Yellow | P2 | 可触发整批抓取，但操作者只能看到逐条 `alert` / 列表刷新，难以判断整批完成情况；见 `IA-SPEC-002`。 |
| Settings | Settings 页面加载 | 打开页面加载 AI 配置 | `frontend/src/pages/Settings.tsx:18-38` | `GET /api/config/ai`（`backend/routers/config.py:10-26`，`response_model=AIConfigResponse`） | PRD:441-445,449-452; TECH:346-350 | 页面进入后请求 AI 配置，并把 `provider/base_url/model` 写入表单；`api_key` 始终置空显示。 | Yellow | P1 | 加载成功时可回填表单，但失败只写 `console.error` 且回退默认值，缺少明确错误态；见 `IA-SPEC-004`。 |
| Settings | Test Connection 按钮 | 点击测试连接 | `frontend/src/pages/Settings.tsx:54-65,182-195` | `POST /api/config/ai/test`（`backend/routers/config.py:41-45`） | PRD:444,452; TECH:346-350 | 先清空旧测试结果并进入 testing 态；返回后展示成功/失败消息。 | Red | P1 | 测试按钮验证的是已保存配置而非当前表单，用户修改后再测试会得到错位结论；见 `IA-BE-007` / `IA-SPEC-003`。 |
| Settings | Save Architecture 按钮 | 点击保存 AI 配置 | `frontend/src/pages/Settings.tsx:40-52,215-221` | `PUT /api/config/ai`（`backend/routers/config.py:29-38`）；成功后 `GET /api/config/ai`（`backend/routers/config.py:10-26`） | PRD:442-445,449-452; TECH:346-350 | 提交当前表单到后端；保存成功后提示“AI 配置已保存”并重新加载配置。 | Red | P1 | 保存会直接提交空 `api_key` 字段，存在覆盖已存密钥并破坏后续生成的风险；见 `IA-BE-007` / `IA-SPEC-003`。 |
