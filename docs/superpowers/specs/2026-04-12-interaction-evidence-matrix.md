# 交互证据矩阵

| 页面 | 控件/入口 | 用户动作 | 前端触发逻辑（文件:行） | 后端接口（Method Path） | 需求依据（文档:行） | 当前行为 | 判定 | 优先级 | 备注 |
|---|---|---|---|---|---|---|---|---|---|
| Newsletter | 日期选择器 / 今天 | 选择历史日期或今天 | `frontend/src/pages/Newsletter.tsx:84-88,109-123,275-285` | `digestsApi.list → GET /digests?limit=30`（`frontend/src/api/newsletter.ts:198-203`）；`digestsApi.getByDate → GET /digests/{date}`（`frontend/src/api/newsletter.ts:208`）；首屏默认 `digestsApi.latest → GET /digests/latest`（`frontend/src/api/newsletter.ts:206`） | 待 Task 4 补充 | 日期下拉组件挂载时先拉取最近 30 天简报日期；选择某个日期/今天后设置 `selectedDate`，再请求对应日期简报；未选择日期时首屏请求 latest。 | 待 Task 5 评定 | 待定 | 头部左右箭头按钮仅渲染样式，当前没有 `onClick`。 |
| Newsletter | 兴趣标签侧栏 | 点击标签筛选简报 | `frontend/src/pages/Newsletter.tsx:24-31,266-273,287-293` | 首屏 `interestsApi.listTags → GET /interests/tags`（`frontend/src/api/newsletter.ts:219`）；点击标签后无新增接口调用 | 待 Task 4 补充 | 页面首屏加载标签列表；点击标签只会更新 `selectedTag` 并在前端内存中过滤 `digest.sections[].insights`。 | 待 Task 5 评定 | 待定 | 当前没有服务端按标签二次筛选。 |
| Newsletter | Main Channel 来源链接 | 点击 `Read Source` | `frontend/src/pages/Newsletter.tsx:201-203` | 无当前 API 调用；当前页未接入 `behaviorApi.recordFeedback → POST /behavior/feedback`（`frontend/src/api/newsletter.ts:261-264`） | 待 Task 4 补充 | 直接打开外部文章来源（新标签页），不会记录前端状态或回传后端点击事件。 | 待 Task 5 评定 | 待定 | 当前链接点击与行为采集解耦。 |
| Newsletter | Main Channel 点赞 / 点踩按钮 | 点击反馈按钮 | `frontend/src/pages/Newsletter.tsx:194-199` | 无当前 API 调用；可用但未接线 `behaviorApi.recordFeedback → POST /behavior/feedback`（`frontend/src/api/newsletter.ts:261-264`） | 待 Task 4 补充 | 按钮仅渲染 UI，当前没有 `onClick` 处理函数，也不会调用任何行为/反馈接口。 | 待 Task 5 评定 | 待定 | 反馈闭环尚未接通。 |
| InterestSettings | Add Interest 输入框 / Add Interest 按钮 | 输入标签后回车或点击添加 | `frontend/src/pages/InterestSettings.tsx:32-44,157-168` | `interestsApi.createTag → POST /interests/tags`（`frontend/src/api/newsletter.ts:223-227`）；成功后 `interestsApi.listTags → GET /interests/tags`（`frontend/src/api/newsletter.ts:219`）+ `interestsApi.getCandidates → GET /interests/candidates?top_n=5`（`frontend/src/api/newsletter.ts:243-244`）重新加载 | 待 Task 4 补充 | 空字符串直接拦截；成功后清空输入框并重新加载标签 / 候选数据。 | 待 Task 5 评定 | 待定 | `loadData` 同时还会请求 `getStats`，但当前页面未消费返回值。 |
| InterestSettings | 兴趣卡片 Freeze 操作 | 点击 Freeze | `frontend/src/pages/InterestSettings.tsx:50-57,220-227` | `interestsApi.updateTag → PATCH /interests/tags/{id}`（`frontend/src/api/newsletter.ts:229-233`）；成功后重新加载 `listTags/getCandidates` | 待 Task 4 补充 | 将标签状态更新为 `frozen`，成功后整页数据刷新。 | 待 Task 5 评定 | 待定 | 操作入口仅在 hover 态出现。 |
| InterestSettings | 冻结兴趣 Thaw / Activate 操作 | 点击 Thaw | `frontend/src/pages/InterestSettings.tsx:50-57,211-218` | `interestsApi.updateTag → PATCH /interests/tags/{id}`（`frontend/src/api/newsletter.ts:229-233`）；成功后重新加载 `listTags/getCandidates` | 待 Task 4 补充 | 将标签状态更新为 `active`，成功后整页数据刷新。 | 待 Task 5 评定 | 待定 | UI 标题写 `Thaw`，实际传值为 `active`。 |
| InterestSettings | 兴趣卡片 Delete 操作 | 点击 Delete 并确认 | `frontend/src/pages/InterestSettings.tsx:59-67,228-233` | `interestsApi.deleteTag → DELETE /interests/tags/{id}`（`frontend/src/api/newsletter.ts:235-236`）；成功后重新加载 `listTags/getCandidates` | 待 Task 4 补充 | 先弹浏览器 `confirm`；确认后删除标签并刷新数据。 | 待 Task 5 评定 | 待定 | 取消确认时不会发请求。 |
| InterestSettings | Emergent Patterns / Adopt | 点击候选标签的 Adopt | `frontend/src/pages/InterestSettings.tsx:46-48,282-287` | `interestsApi.createTag → POST /interests/tags`（`frontend/src/api/newsletter.ts:223-227`）；成功后重新加载 `listTags/getCandidates` | 待 Task 4 补充 | `handleAddCandidate` 直接复用 `handleAddTag`；点击后把候选标签转成正式兴趣标签。 | 待 Task 5 评定 | 待定 | 当前没有单独“采纳候选”接口。 |
| Sources | 页脚 Quick Add 输入框 / 添加按钮 | 粘贴公众号文章链接后回车或点击添加 | `frontend/src/pages/Sources.tsx:33-57,279-297` | `sourcesApi.parseUrl → POST /sources/parse-url`（`frontend/src/api/newsletter.ts:168-172`） | 待 Task 4 补充 | 先做非空和 `mp.weixin.qq.com` 校验；解析成功后打开确认弹窗并注入 `parsedAccountForModal`，此步骤本身不创建 source。 | 待 Task 5 评定 | 待定 | 解析失败仅在页脚输入区显示错误。 |
| Sources | Quick Add 弹窗 / 解析 | 在弹窗内点击解析 | `frontend/src/pages/Sources.tsx:483-506,541-562` | `sourcesApi.parseUrl → POST /sources/parse-url`（`frontend/src/api/newsletter.ts:168-172`） | 待 Task 4 补充 | 当弹窗没有 `initialAccount` 时，用户可再次输入 URL 并重新解析公众号信息。 | 待 Task 5 评定 | 待定 | 与页脚 Quick Add 共用同一后端解析接口。 |
| Sources | Quick Add 弹窗 / 确认添加 | 点击确认添加公众号 | `frontend/src/pages/Sources.tsx:508-526,627-633` | `sourcesApi.create → POST /sources`（`frontend/src/api/newsletter.ts:146-150`） | 待 Task 4 补充 | 将解析出的公众号信息映射成 `mptext` source 并创建；成功后关闭弹窗并重新加载 source 列表。 | 待 Task 5 评定 | 待定 | `api_base_url` / `auth_key` 使用前端写死默认值。 |
| Sources | SourceModal / 新增来源保存 | 点击 Add New Source 或空状态 Start Connection 后提交保存 | `frontend/src/pages/Sources.tsx:116-119,177-180,355-377,392-463` | `sourcesApi.create → POST /sources`（`frontend/src/api/newsletter.ts:146-150`） | 待 Task 4 补充 | 打开 `SourceModal` 后提交表单创建 source；成功后关闭弹窗并重新加载列表。 | 待 Task 5 评定 | 待定 | 这是“手动新增来源”路径，与 Quick Add 分开。 |
| Sources | Source 行内 Edit Source | 点击编辑并保存 | `frontend/src/pages/Sources.tsx:82-85,244-250,355-377,392-463` | `sourcesApi.update → PUT /sources/{id}`（`frontend/src/api/newsletter.ts:152-156`） | 待 Task 4 补充 | 行内编辑按钮打开 `SourceModal`；提交时如果存在 `source` 则走更新接口。 | 待 Task 5 评定 | 待定 | 更新失败仅弹浏览器 `alert`。 |
| Sources | Source 行内 Delete Source | 点击删除并确认 | `frontend/src/pages/Sources.tsx:59-67,251-257` | `sourcesApi.delete → DELETE /sources/{id}`（`frontend/src/api/newsletter.ts:158-161`） | 待 Task 4 补充 | 先弹浏览器 `confirm`；确认后删除并直接用本地 `filter` 更新列表。 | 待 Task 5 评定 | 待定 | 删除成功后不会重新拉取列表。 |
| Sources | Source 行内 Fetch Now / Manual Crawl All | 点击单条抓取或整页批量抓取 | `frontend/src/pages/Sources.tsx:69-80,108-114,236-243` | `sourcesApi.fetch → POST /sources/{id}/fetch`（`frontend/src/api/newsletter.ts:163-166`） | 待 Task 4 补充 | 单条抓取会显示返回消息并重新拉取列表；`Manual Crawl All` 通过 `sources.forEach` 逐条调用同一接口。 | 待 Task 5 评定 | 待定 | 批量抓取没有串行/并发控制，也没有总进度反馈。 |
| Settings | Settings 页面加载 | 打开页面加载 AI 配置 | `frontend/src/pages/Settings.tsx:18-38` | `configApi.getAI → GET /config/ai`（`frontend/src/api/newsletter.ts:177`） | 待 Task 4 补充 | 页面进入后请求 AI 配置，并把 `provider/base_url/model` 写入表单；`api_key` 始终置空显示。 | 待 Task 5 评定 | 待定 | 加载失败仅 `console.error`，页面不会展示错误态。 |
| Settings | Test Connection 按钮 | 点击测试连接 | `frontend/src/pages/Settings.tsx:54-65,182-195` | `configApi.testAI → POST /config/ai/test`（`frontend/src/api/newsletter.ts:185`） | 待 Task 4 补充 | 先清空旧测试结果并进入 testing 态；返回后展示成功/失败消息。 | 待 Task 5 评定 | 待定 | 当前测试请求不携带表单临时修改值，只测试后端已保存配置。 |
| Settings | Save Architecture 按钮 | 点击保存 AI 配置 | `frontend/src/pages/Settings.tsx:40-52,215-221` | `configApi.updateAI → PUT /config/ai`（`frontend/src/api/newsletter.ts:179-183`）；成功后 `configApi.getAI → GET /config/ai`（`frontend/src/api/newsletter.ts:177`） | 待 Task 4 补充 | 提交当前表单到后端；保存成功后提示“AI 配置已保存”并重新加载配置。 | 待 Task 5 评定 | 待定 | 保存后会重置 `api_key` 展示为空字符串。 |
